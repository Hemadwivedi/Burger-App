<p align="center">
    {{#if devoured}}
        {{id}}.
        {{burger_name}}

    {{else}}
        {{burger_name}}
        <button class="change-devour" data-id="{{id}}">DEVOUR</button>
    {{/if}}

</p>
